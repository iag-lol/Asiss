name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build
        env:
          # Solo MiniCheck recibe credenciales desde GitHub Actions.
          # El resto de ASISS usa la configuración principal definida en el código.
          VITE_MINICHECK_SUPABASE_URL: ${{ secrets.VITE_MINICHECK_SUPABASE_URL }}
          VITE_MINICHECK_SUPABASE_ANON_KEY: ${{ secrets.VITE_MINICHECK_SUPABASE_ANON_KEY }}

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
